# Sentry DSN for error tracking and performance monitoring.
# Leave empty to disable Sentry (the app runs without it locally).
# Obtain from your Sentry project settings; never commit a real DSN.
VITE_SENTRY_DSN=
